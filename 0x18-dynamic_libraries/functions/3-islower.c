#include "main.h"

/**
 * _islower - checks for lowercase character in a string
 * @d: the character to be checked
 *
 * Return: 1 if d is lowercase, 0 otherwise
 */
int _islower(int d)
{
	return (d >= 'a' && d <= 'z');
}
