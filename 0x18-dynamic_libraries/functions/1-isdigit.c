#include "main.h"

/**
 * _isdigit - finds a digit in range 0-9
 * @i: the integer which is to be checked
 * Return: 1 when c is a digit, else return 0
 */
int _isdigit(int i)
{
return (i >= '0' && i <= '9');
}
